name: Testando a regra de create

on:
  create

jobs:
  print-branch-or-tag-creation: #job name
    runs-on: ubuntu-latest

    steps:
      - name: Aviso sobre criação de branches ou tags
        runs: echo "Uma nova Branch ou Tag foi criada no repositório"
  
name: Teste trigger Issues

on:
  issues: 
    types: [opened, editer, deleted]

jobs:
  Teste-trigger-issue:
    runs-on: ubuntu-latest
    steps:
      - name: Teste trigger issues
        run: echo "Uma issue foi aberta/editada/deletada"